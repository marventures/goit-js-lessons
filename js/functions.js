const a = 1;
const b = 2;
const c = 3;

console.log(a + b + c);

// Functions

// How to declare a function?
// function keyword
// function name
// function parameters -> serves as a placeholder or acts as a variable for that function
function addMe(a, b, c) {
  console.log(a + b + c);
}

// call a function
addMe(1, 2, 3);
addMe(2, 4, 5);
